"use client";
import {ResponsiveContainer,BarChart,Bar,XAxis,YAxis,Tooltip,CartesianGrid} from "recharts";
export default function Chart({title,rows,currency=false}:{title:string;rows:{label:string;value:number}[];currency?:boolean}){return <section className="card chart"><h3>{title}</h3>{!rows.length?<p className="empty muted">Todavía no hay datos para este período.</p>:<><div style={{width:"100%",height:230}}><ResponsiveContainer><BarChart data={rows} margin={{left:0,right:8,bottom:15}}><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="#e5e6e3"/><XAxis dataKey="label" tick={{fontSize:10}}/><YAxis width={55} tick={{fontSize:10}} tickFormatter={n=>currency?"$"+(n>=1000?n/1000+"k":n):String(n)}/><Tooltip/><Bar dataKey="value" name={currency?"Ingresos ($)":"Cortes"} fill="#58705c" radius={[5,5,0,0]}/></BarChart></ResponsiveContainer></div><details><summary>Ver datos</summary><div className="table-wrap"><table><tbody>{rows.map(r=><tr key={r.label}><td>{r.label}</td><td>{currency?"$":""}{r.value.toLocaleString("es-AR")}</td></tr>)}</tbody></table></div></details></>}</section>}